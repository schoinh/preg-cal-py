# preg-cal-py

For dev, use a venv:

```
python -m venv venv
source venv/bin/activate
```

Then install dependencies:

```
pip install -r requirements.txt
```

To run web app:

```
python app.py
```

To run CLI:

```
python cli.py
```
